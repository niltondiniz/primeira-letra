import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:primeira_letra/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // FinishController finish;
  //
  setUp(() {
    //     finish = HomeModule.to.get<FinishController>();
  });

  group('FinishController Test', () {
    //   test("First Test", () {
    //     expect(finish, isInstanceOf<FinishController>());
    //   });

    //   test("Set Value", () {
    //     expect(finish.value, equals(0));
    //     finish.increment();
    //     expect(finish.value, equals(1));
    //   });
  });
}
